

//Other function
backend để search từng place
frontend hiện trang search (Từng place là card vs tên + nước -> nhấn vô sẽ popup trang bự/đẩy qua trang khác ghi đủ thông tin)

hỏi "bạn thân" xem có cần thêm chức năng gì nữa k

//Optional - Khó hơn
- (Sau cai tren) See how to apply AHP to list for each parameter first

//query 
- distinct tag: 
SELECT DISTINCT JSON_UNQUOTE(JSON_EXTRACT(tag, '$')) AS tag
FROM Places,
JSON_TABLE(tags, '$[*]' COLUMNS (tag JSON PATH '$')) AS jt
ORDER BY tag;

Link tới trang du lịch để hiện data đúng, k có mock nữa
số tiền
dịch vụ
phương pháp vân chuyển
dịch vụ thêm


task 1/12:
light dark mode - p4
login / sign up - p3 + trang user/admin - p3
learn more - p2 (đi cùng vs sửa lại landing)
chỉnh trang result lại -p2
cho search và filter trong search place - p1
add hình - p2


todo 9/12
- only reload content, k load nav bar ở trên




/todo 17/12
continue to try doing web crawling
authentication (both fe and be)
    - not login: can use system normally
    - login: can view history, mark fav
    - admin: add place manually + crawl web (see how to do it with python BE)
fix front
    - add picture (w/ cloudinary)
    - change result page to better reflect result
    - lúc search -> madives lỗi (chắc do nằm trong ngoặc)
fix backend
    - better algorithm?
add missing value to .env